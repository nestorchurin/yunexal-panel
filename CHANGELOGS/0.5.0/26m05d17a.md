## Snapshot - 17.05.2026 - 0.0.0-snapshot-26m05d17a

### Hotfix: New server create modal crash from missing optional hook
- `static/js/new_server.js`:
	- added a backward-compatible no-op bootstrap for `window._refreshSrvPortSelect` so stale or partial integrations cannot crash `updateYaml()`
	- kept the runtime call guarded as optional integration behavior
- `templates/new_server.html`:
	- bumped script asset version to `new_server.js?v=2` so clients stop using stale cached bundles that still call the missing symbol directly
