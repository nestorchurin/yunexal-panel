## Snapshot - 11.4.2026 - 0.0.0-snapshot-26m04d13a (Where 12 - day, 4 - month, 2026 - year, 542026 - commit hash prefix)

### Setup wizard compatibility — OpenRC + systemd
- `src/bin/setup.rs`:
	- прибрано Alpine-only блок, додано авто-детекцію init-системи (`OpenRC`/`systemd`) і package manager (`apk`/`apt-get`)
	- Docker/nginx/service flow переведено на platform-aware команди (`rc-service/rc-update` або `systemctl`)
	- додано генерацію systemd unit (`/etc/systemd/system/yunexal-panel.service`) паралельно з існуючим OpenRC flow
	- nginx setup тепер створює конфіг у правильному шляху для кожної системи (`http.d` або `sites-available` + symlink)
- `README.md`:
	- оновлено інструкції інсталяції/запуску/nginx/SSL під Alpine(OpenRC) і Debian/Ubuntu(systemd)
	- requirements і build notes синхронізовано під дві системи

### Users identity hardening — uid length 9..16 + auto-generation
- `src/db/mod.rs`:
	- users schema для нових інсталяцій посилено: `uid` має бути `9..16` символів (DB `CHECK`)
	- migration/backfill для legacy БД тепер нормалізує некоректні/порожні uid у формат `#u00000000000000` (гарантована довжина 16)
	- DB validation triggers (`insert/update`) доповнено правилом `uid must be 9-16 characters`
	- `seed_root_user(...)` адаптовано, щоб дефолтний uid завжди відповідав `9..16`
- `src/handlers/admin.rs` + `static/js/admin.js` + `templates/admin.html`:
	- backend/frontend валідація create-user синхронізована на межі `uid 9..16`
	- форма create-user переведена на авто-генерацію uid без ручної кнопки
	- покращені user-facing помилки для порушення uid-діапазону

### Server members UX — add by UID only + copyable UID
- `src/handlers/servers.rs` + `src/db/users.rs`:
	- API `POST /api/servers/{id}/members/add` переведено з `user_id` на строгий lookup по `uid`
	- додано DB helper `find_user_by_uid(...)`
- `static/js/server_users.js` + `templates/server_users.html`:
	- Add Member працює тільки через повний UID
	- uid у members-листі та editor header винесено окремим сірим рядком нижче nickname
	- клік по uid копіює значення в буфер; додано короткий локальний бейдж `copied`
- `static/css/server_users.css` + `templates/server_users.html`:
	- виправлено layout-regression (помилковий закриваючий тег + стабілізація позиціонування copied-бейджа)

### Server permission UX — hide Users tab when `members=none`
- `src/handlers/templates.rs`:
	- у server templates додано прапорець `can_members` для керування sidebar-видимістю
- `templates/_server_sidebar.html`:
	- пункт `Users` відображається тільки якщо `can_members == true`
- `src/handlers/servers.rs` + `src/handlers/network.rs` + `src/handlers/files.rs`:
	- `can_members` прокинуто в усі server-page render contexts
	- для прямого обходу URL `/servers/{id}/users` повертається явне повідомлення про відсутність доступу

### Dashboard visibility regression fix (`/`)
- `src/handlers/dashboard.rs` + `src/handlers/templates.rs` + `templates/index.html` + `static/js/dashboard.js`:
	- виправлено конфлікт owner-фільтра `My only` після переходу на nickname-first owner display
	- owner-фільтр тепер застосовується тільки для admin-контролу (коли toggle присутній)
	- frontend cards/API payload синхронізовано по полю `owner`, щоб не ховати доступні сервери помилково

### Hash navigation refactor — прибрано route-hash side effects
- `static/js/footer.js`:
	- видалено глобальну логіку автогенерації route-hash (`/#...`) для внутрішніх лінків
	- додано cleanup legacy route-hash на завантаженні сторінки
	- `yuGo/yuRouteUrl` тепер не додають hash автоматично, лише чистять legacy випадки
- `static/js/files.js`:
	- hash parser для Files приймає тільки валідний path-hash (декодований рядок має починатися з `/`)
	- сторонні/legacy hash-фрагменти автоматично прибираються з URL
- `templates/_script_footer.html` + `templates/files.html`:
	- оновлено cache-bust версії include-скриптів (`footer.js`, `files.js`) для моментального застосування нової hash-логіки

### Users DB refactor — uid + nickname with migration
- `src/db/mod.rs` + `src/db/users.rs`:
	- users-модель розширено полями `uid` (довільний формат, унікальний) і `nickname` (до 24 символів)
	- додано migration для legacy БД: `ALTER TABLE` + backfill (`uid`/`nickname`) + унікальний індекс `idx_users_uid`
	- додано DB-тригери валідації (`uid` required, `nickname` required + max 24)
	- оновлено user-запити (`list/find/create`) під нову схему
- `src/handlers/auth.rs`:
	- логін тепер приймає `username` або `uid`; у session cookie зберігається canonical `username`
- `src/handlers/templates.rs` + `src/handlers/admin.rs` + `templates/admin.html` + `static/js/admin.js`:
	- Admin Users UI/API перероблено під `uid + nickname + username`
	- create user тепер вимагає `uid`, `nickname`, `username`, `password`
	- users table доповнено колонками `UID`/`Nickname`, role/password actions переведено на безпечний row-based display label
- `src/handlers/servers.rs` + `src/db/servers.rs` + `static/js/server_users.js`:
	- members API повертає `uid`/`nickname`/`display_name`
	- Users-панель контейнера перейшла на nickname-first пошук і відображення member-ів у форматі `nickname + uid`
- `src/handlers/create.rs` + `src/handlers/dashboard.rs` + `templates/new_server.html` + `templates/admin_edit.html`:
	- owner-select списки користувачів показують `nickname + uid` для узгодженого UX по всій панелі

### Admin RBAC — tri-state role editor and immutable root
- `src/db/roles.rs`:
	- додано tri-state policy-модель прав (`read | none | write`) через `role_permission_policy`
	- перевірки прав доступу розділено на `role_has_read_permission(...)` і `role_has_write_permission(...)`
- `src/handlers/templates.rs` + `src/handlers/admin.rs`:
	- API оновлення ролі перейшло на payload-мапу `permission -> mode`
	- додано guard `Root role is immutable` на backend для заборони змін ролі `root`
	- додано entry route для редагування ролі: `/admin/roles/{name}/edit`
- `src/handlers/admin.rs` + `src/auth.rs`:
	- таби адмінки та fallback-навігація переведені на `read`-рівень доступу (не тільки `write`)
- `templates/admin.html` + `static/js/admin.js`:
	- Roles вкладка перероблена в новий `Role Studio` дизайн: окремий directory ролей + фокусований editor
	- вибір ролі для редагування тепер працює через in-page state та `history.pushState/replaceState` без повного перезавантаження сторінки
	- прибрано верхній dropdown `Select role to edit...` (щоб не дублювати селектори ролі)
	- role matrix переведено з checkbox на 3-стан dropdown-редагування (`read`, `none`, `write`)
	- editor ролей переведено на single-role режим: назва ролі вибирається через dropdown у самому вікні редагування
	- додано зміну кольору ролі (color picker) з бекенд-збереженням
	- у заголовку вкладки ролей додано selector, який веде на сторінку редагування обраної ролі
	- роль `root` приховано зі списків редагування ролей (і selector-а редагування)
	- у вкладках `Roles` і `Users` допоміжні картки (Create/Directory) перенесено у праву колонку на desktop, за аналогією з `Panel Settings`
	- у topbar бейдж `Admin` замінено на реальну роль поточного користувача з рольовим кольором із БД
	- для `root` додано окремий color picker у Roles sidebar (збереження без перезавантаження сторінки)
- `src/db/roles.rs` + `src/handlers/admin.rs` + `src/handlers/templates.rs`:
	- додано поле `roles.color` (з авто-міграцією для існуючих БД)
	- API ролей тепер повертає `color` і приймає `color` в update payload
	- API `POST /api/admin/roles/root/permissions` тепер дозволяє root-only оновлення лише кольору ролі `root`; policy лишається immutable
	- прибрано forced reset кольору `root` під час ініціалізації schema, щоб кастомний root color зберігався після рестарту

### Admin RBAC — Roles tab, create roles, and full permission matrix
- `templates/_admin_sidebar.html`:
	- додано пункт `Roles` у сайдбарі адмін-панелі
- `templates/admin.html`:
	- додано нову вкладку `Roles & Permissions` для створення ролей та керування правами доступу
	- у вкладці `Users` role selector переведено на динамічне завантаження ролей
	- для кожного non-root користувача додано зміну ролі прямо в таблиці
- `static/js/admin.js`:
	- реалізовано повний client-side workflow RBAC:
		- `rolesLoad()` — завантаження ролей і permission catalog
		- `createRole()` — створення ролі
		- `saveRolePermissions()` — оновлення permission matrix ролі
		- `deleteRole()` — видалення ролі
		- `setUserRole()` — зміна ролі користувача
	- інтегровано автозавантаження role-data для вкладок `Users`/`Roles`
- `src/db/roles.rs` (новий файл):
	- додано схему `roles` + `role_permissions`
	- додано permission catalog для адмін-доступів (вкладки, users/images/dns/audit/settings/storage/security/theme, server/container права)
	- реалізовано CRUD-операції для ролей і прав, перевірки валідності role/permission, і перевірку `role_has_permission(...)`
- `src/db/mod.rs`:
	- підключено `roles` модуль та ініціалізацію roles schema в `init_db()`
- `src/db/users.rs`:
	- додано `update_user_role(...)`
- `src/auth.rs`:
	- middleware `require_admin` переведено на permission-aware контроль доступу
	- додано мапінг route -> required permission
	- `/admin` тепер враховує granular permissions
	- `can_access_server(...)` підтримує `servers.global_access`
- `src/handlers/admin.rs`:
	- додано `roles` у `VALID_TABS`
	- `admin_page` і `admin_tab_page` тепер маршрутизують користувача на першу дозволену вкладку
	- додано API для RBAC:
		- `GET /api/admin/roles`
		- `POST /api/admin/roles`
		- `POST /api/admin/roles/{name}/permissions`
		- `POST /api/admin/roles/{name}/delete`
		- `POST /api/admin/users/{id}/set-role`
	- `api_create_user` тепер підтримує custom roles із валідацією існування ролі
- `src/handlers/mod.rs`:
	- додано роутинг нових API для ролей і призначення ролей користувачам
- `src/handlers/templates.rs`:
	- додано DTO форми для role-management API (`CreateRoleForm`, `SetRolePermissionsForm`, `SetUserRoleForm`)

### New Server — Docker image local-first
- `src/handlers/create.rs`: у `create_server` змінено порядок резолву образу — спочатку local inspect (`docker::get_image_info`), pull виконується лише якщо образ відсутній локально
- `src/handlers/create.rs`: додано `valid_image_ref(...)` для базової серверної валідації image tag/reference

### New Server — build image з Dockerfile
- `src/handlers/create.rs`: додано `api_build_image_from_dockerfile(...)`:
	- приймає multipart (`image`, `dockerfile`)
	- валідує тег і розмір Dockerfile
	- створює тимчасовий build context
	- запускає `docker build -t <tag> -f Dockerfile .`
	- пише audit log `image.build_dockerfile`
- `src/handlers/mod.rs`: підключено admin route `POST /api/image/build-dockerfile` (+ `DefaultBodyLimit::disable()`)

### New Server UI — Basic Info переробка
- `templates/new_server.html`: блок Docker Image переформатовано в більш читабельну структуру, додано окремий підблок `Build Custom Local Image`
- `templates/new_server.html`: додано поле `Custom image tag (optional)` для явного іменування кастомного образу
- `static/css/new_server.css`: додані стилі для нового layout Basic Info (`image-tools-row`, `dockerfile-builder`, `inline-status`)

### Docker Image picker — dropdown прямо в input
- `templates/new_server.html`: прибрано окремий select; dropdown інтегровано прямо в поле `Docker Image` (toggle-кнопка + popup-list)
- `static/js/new_server.js`: реалізовано in-input dropdown логіку:
	- завантаження локальних тегів з `/api/image/local`
	- фільтрація під час набору
	- open/focus, toggle/click, close/blur/outside-click
	- вибір елемента кліком в інпут
- `static/css/new_server.css`: стилі для in-input dropdown (`image-dd-toggle`, `image-dropdown`, `image-dropdown-item`, `image-dropdown-empty`)

### Синхронізація кастомно зібраних образів
- `static/js/new_server.js`: після успішного Dockerfile build новий tag одразу додається в локальні підказки та dropdown
- `static/js/new_server.js`: після build tag автоматично підставляється в `Docker Image`

### Draft persistence форми
- `static/js/new_server.js`: у `saveFormState/restoreFormState` додано збереження/відновлення `dockerfile-image-tag`

### CSP hardening
- `templates/files.html`: додано nonce + SRI для HTMX script include, щоб прибрати CSP block для `script-src-elem`

### Frontend dependency include
- `templates/_head_common.html`: додано глобальний Cloudflare jQuery include `3.5.1` (SRI + `crossorigin` + `nonce`) для уніфікованого підключення на основних сторінках панелі

### Storage policy hardening — заборона system/critical volumes та ext4 без prjquota
- `src/handlers/admin.rs`: додано централізовану валідацію `validate_storage_base_path(...)`:
	- блокує critical mount points (`/`, `boot/efi`, `usr/var/etc`, `bin/sbin/lib*`)
	- блокує `ext4` без `prjquota/prjjquota`
	- блокує шляхи на системному root-диску (same source / same top parent block device)
- `src/handlers/admin.rs`: `api_admin_set_setting(...)` тепер відхиляє небезпечний `container_storage_path` ще до запису в settings
- `src/handlers/admin.rs`: `api_admin_storage_migrate(...)` тепер валідовує `target_base_path` тією ж policy-перевіркою
- `src/handlers/admin.rs`: `api_admin_storage_mounts(...)` тепер віддає тільки дозволені mount targets + метадані `default_allowed/default_reason/default_path`
- `static/js/admin.js`: admin storage picker адаптовано під policy-метадані:
	- якщо default path заборонений, він позначається як `blocked` і не обирається
	- текст тригера показує причину блокування від бекенду
- `static/css/admin.css`: додано стилі disabled-стану для заблокованого default option у picker

### Quota enforcement hard block (new server)
- `static/js/new_server.js`:
	- preflight quota banner переведено з warning у hard-block стан (коли `quota-check` не проходить)
	- кнопки `Create Server` (основна + modal confirm) disabled, поки quota unsupported
	- direct submit/confirm тепер теж блокується guard-логікою до налаштування квот
	- текст `ext4 without prjquota` змінено на blocking-повідомлення
- `templates/new_server.html`:
	- оновлено текст banner на блокуючий
	- додано ID для create-кнопок, щоб керувати disabled-станом з JS
- `src/handlers/create.rs`:
	- якщо заданий `disk_limit` і вибраний storage path не quota-capable (не `xfs+prjquota`, не `ext4+prjquota`, не `zfs`, не `btrfs`) — створення повертає помилку (hard fail), а не warning-only

### Unsafe override mode (Admin Settings, at your own risk)
- `templates/admin.html`:
	- у вкладці `admin/settings` додано toggle `Unsafe storage override (at your own risk)`
	- при ввімкненні показується confirm із попередженням про ризики
	- після зміни toggle UI автоматично перезавантажує storage mounts/disks
- `src/handlers/templates.rs` + `src/handlers/admin.rs`:
	- додано template-прапорець `settings_storage_unsafe_override`
	- ключ `storage_unsafe_override` додано у panel settings API (`/api/admin/settings`)
- `src/handlers/admin.rs` (storage API bypass у unsafe режимі):
	- `api_admin_storage_mounts`: policy-валідація (critical/root/ext4-no-prjquota) може бути bypass для custom-path сценаріїв, при цьому UI-список залишено в межах релевантних FS томів
	- `api_admin_storage_disks` / `api_admin_storage_change_fs`: список partitions повернено до non-system/eligible відображення як раніше
	- `api_admin_storage_migrate`: target path більше не блокується policy-валидацією
- `src/handlers/create.rs` + `static/js/new_server.js`:
	- create flow і quota preflight поважають `storage_unsafe_override`
	- при увімкненому unsafe override hard-block знімається, створення/вибір storage дозволені з попередженням `at your own risk`

### Dropdown UX fix (Storage selectors on scroll/mobile)
- `static/js/admin.js`:
	- для dropdown `Default container storage location` додано live reposition на `scroll/resize` (включно з `visualViewport` для мобільних)
	- dropdown залишається прив’язаним до trigger-поля під час прокрутки сторінки
- `static/js/new_server.js`:
	- аналогічний live reposition додано для storage selector у New Server
	- при закритті dropdown коректно відв’язуються listeners, щоб уникнути дрейфу позиції
- `static/css/admin.css` + `static/css/new_server.css`:
	- `stor-sel-panel` переведено на `overflow-y: auto` + `touch-action: pan-y`, щоб скрол у списку працював коректно на телефоні

### Disk filesystem conversion — ZFS додано
- `templates/admin.html`:
	- у `Disk filesystem conversion` додано опцію `zfs` в selector
	- опис секції оновлено: для `zfs` створюється новий ZFS pool на вибраному partition
- `src/handlers/admin.rs` (`api_admin_storage_change_fs`):
	- додано підтримку `fs_type = zfs`
	- замість `mkfs` виконується `zpool create -f` з параметрами (`compression=lz4`, `atime=off`, mountpoint)
	- у відповідь повертаються `zfs_pool` і `zfs_mountpoint`
	- audit detail для `storage.change_fs` містить створений pool/mountpoint
	- `storage_sudo_fix_command` доповнено `zpool` у дозволених sudo-командах
- `static/js/admin.js`:
	- success UI для conversion показує окремий блок із назвою створеного ZFS pool та mountpoint

### Admin Settings — категорійний редизайн
- `templates/admin.html`:
	- вкладку `/admin/settings` перероблено на категорійний layout (`Storage`, `Security`, `Operations`, `Interface`) з окремою навігацією
	- додано динамічний intro-блок категорії (kicker/title/description), який оновлюється при перемиканні
	- існуючі settings-панелі перенесені в категорії через `data-settings-cat` без зміни їхніх `id`/hook’ів (збережена сумісність із поточним JS)
- `static/css/admin.css`:
	- додано повний стилевий шар для нового категорійного settings-UI (sticky nav, active states, animated panel switch, responsive mobile horizontal nav)
	- категорійний режим активується через клас `settings-categorized`, щоб без JS сторінка залишалась деградаційно читабельною
	- desktop layout уточнено: блок `Settings Categories` перенесено на праву колонку
	- mobile polish: кнопки категорій і іконки зменшено для компактного відображення на телефоні
- `static/js/admin.js`:
	- додано `settingsSwitchCategory(...)` і `initSettingsCategories()`
	- інтегровано ініціалізацію категорій у lifecycle admin tabs (`switchTab`, `popstate`, direct-load)
	- вибір категорії зберігається в `sessionStorage` між переходами у межах сесії

### Global navigation hash markers (`/#where-user-goes`)
- `static/js/footer.js`:
	- додано глобальні helper-и `yuRouteUrl(...)` та `yuGo(...)`, які для внутрішніх page-route URL додають hash-маркер напрямку (`/path#path`)
	- додано авто-анотацію всіх внутрішніх `<a href="/...">` через `MutationObserver` (покриває також динамічно згенеровані лінки)
	- API/static/assets URL виключені з hash-анотації, щоб не чіпати технічні endpoint-и
	- `/admin` і `/admin/*` виключені з hash-анотації (admin URL тепер без hash)
	- після повного перегляду route-поведінки додано додаткові no-hash винятки для utility/auth сторінок: `/login`, `/logout`, `/servers/new`
	- для Files-переходів посилено винятки (`/servers/{id}/files`, `/servers/{id}/files/edit`, включно з trailing slash), щоб глобальний route-hash не конфліктував з директорійним hash у file browser
	- для всіх лінків усередині `.yu-sidebar` вимкнено hash-анотацію: sidebar-навігація тепер завжди використовує чисті URL без `#...`
- `templates/_script_footer.html`:
	- оновлено include до `footer.js?v=4`, щоб нова глобальна навігаційна логіка одразу застосувалась без кеш-артефактів
	- оновлено include до `footer.js?v=5`, щоб застосувати sidebar no-hash поведінку без кешу

### Server sidebar SPA navigation (no full page reload)
- `static/js/sidebar.js`:
	- додано client-side SPA-навігацію для server sidebar маршрутів (`/servers/{id}/console|files|networking|settings|audit`) через `fetch + history.pushState`
	- при переході замінюється лише `.yu-main`, а не вся сторінка; `title` і active-state sidebar синхронізуються без full reload
	- додано завантаження відсутніх head assets і body page-scripts для цільової вкладки під час першого переходу
	- додано подію `yu:page-shown`, щоб сторінкові скрипти могли перевантажувати дані при поверненні з кешу SPA
	- виправлено порядок active `.yu-main` у layout: активна вкладка тепер стає першою після sidebar, щоб уникнути конфліктів селекторів при кешованих вкладках
	- механіку sidebar перероблено під admin-like flow: явний `serverSwitchTab(...)`, синхронний `pushState/popstate`, і лише один активний `.yu-main` у DOM (інші вкладки кешуються поза DOM), що прибирає desktop-конфлікти duplicated id
	- клік по вже активній вкладці тепер тригерить `yu:page-shown` refresh-hook (аналогічно до повторного refresh у tab UX)
- `templates/_server_sidebar.html`:
	- server nav links переведено на inline `serverSwitchTab(...)` (як в admin sidebar через tab-switch handler) з fallback на стандартний `href`
- `templates/_scripts_server.html`:
	- оновлено include до `sidebar.js?v=5` для cache-bust після повного рефакторингу sidebar
- `templates/settings.html`:
	- ініціалізацію `envRenderList` зроблено сумісною з динамічним SPA-load (не лише через `DOMContentLoaded`)
- `templates/_scripts_server.html`:
	- оновлено include до `sidebar.js?v=3` для cache-bust нової SPA-навігації

### Container audit log (server-level)
- `src/db/audit.rs`:
	- додано `audit_list_for_server(...)` і `audit_count_for_server(...)` для фільтрації audit entries по конкретному контейнеру (`#<server_id>` / `server=<id>`)
- `src/handlers/servers.rs` + `src/handlers/mod.rs`:
	- додано сторінку `GET /servers/{id}/audit`
	- додано API `GET /api/servers/{id}/audit` з пагінацією, фільтрами `action/search/actor` і перевіркою доступу через `can_access_server(...)`
	- дефолт `limit` у server audit API змінено на `50` (fallback, якщо клієнт не передає `limit`)
	- додано експорт `GET /api/servers/{id}/audit/download` для скачування повного audit у форматі `.log` (без пагінації)
- `src/handlers/templates.rs` + `templates/server_audit.html` + `static/js/server_audit.js`:
	- додано окремий container audit UI “як в admin”: таблиця подій, пошук, multi-select action filter dropdown, paginated list, badge-стилі дій і device/UA колонка
	- додано кнопку `Download .log` у toolbar та client-side flow завантаження файлу з поточними фільтрами

### API access for third-party services
- `src/auth.rs`:
	- додано перевірку service API key з заголовків (`Authorization: Bearer ...` або `X-API-Key`)
	- key резолвиться з `panel_settings.service_api_key`, або fallback з env (`YUNEXAL_API_KEY` / `PANEL_API_KEY`)
- `src/handlers/auth.rs` + `src/handlers/mod.rs`:
	- додано endpoint `POST /api/auth/service-login`, який видає звичайну panel session cookie для сторонніх сервісів після валідації API key
	- далі сервіс може викликати існуючі API endpoint-и з цією cookie без браузерного логіну
- `src/handlers/admin.rs`:
	- ключ `service_api_key` додано в allowlist panel settings API

### Container-scoped user access (without roles)
- `src/db/mod.rs` + `src/db/servers.rs` + `src/db/users.rs`:
	- додано таблицю `server_user_permissions` (`server_id + user_id + permission + mode`), policy-модель `none|read|write`
	- додано helper-и для додавання member-ів до контейнера, зміни policy, перевірок read/write і видалення доступу
	- при видаленні user/server тепер очищаються пов’язані записи container-permissions
- `src/auth.rs`:
	- `can_access_server(...)` тепер враховує owner + явний container access для user
	- додано `can_access_server_permission(...)` для capability-check по конкретній категорії контейнера
- `src/handlers/dashboard.rs` + `src/handlers/ws.rs`:
	- не-адмін користувачі бачать на dashboard/stats не лише owned-контейнери, а й ті, де вони додані як member-и
- `src/db/servers.rs`:
	- `list_accessible_container_ids(...)` оновлено: контейнер відображається на `/` одразу після додавання user-а в member-и (visible-by-membership)
- `src/handlers/servers.rs` + `src/handlers/mod.rs`:
	- додано API керування доступом до конкретного контейнера:
		- `GET /api/servers/{id}/members`
		- `POST /api/servers/{id}/members/add`
		- `POST /api/servers/{id}/members/{user_id}/permissions`
		- `POST /api/servers/{id}/members/{user_id}/remove`
	- доступ до page/API прив’язано до container-capabilities: `console/files/networking/settings/audit`, а power-actions (`start/stop/restart/kill`) — до `power:write`
- `src/handlers/templates.rs` + `src/handlers/servers.rs` + `src/handlers/mod.rs` + `templates/_server_sidebar.html`:
	- додано окрему серверну вкладку `Users` (`/servers/{id}/users`) у sidebar для керування доступами до конкретного контейнера
	- management container-member прав перенесено з Console у нову сторінку `Users`
- `templates/server_users.html` + `static/js/server_users.js` + `static/css/server_users.css`:
	- реалізовано full-flow керування доступами user-ів до контейнера (`none/read/write`) в окремій сторінці
	- замість `select user` додано поле пошуку за нікнеймом (nickname) з підказками і додаванням через Enter/Add
	- сторінку `Users` перероблено в `roles-studio` style (directory member-ів + окремий editor прав), візуально узгоджено з дизайном Admin Roles
	- доступні операції: add member, change permission policy, remove member
- `templates/console.html` + `static/js/console.js`:
	- прибрано вбудований блок `Container Access` з Console, залишено console-only UX (terminal/stats/power)

### Files modal runtime fix
- `static/js/files.js`:
	- виправлено падіння `bootstrap.Modal` (`this._config is undefined`) при створенні файлів/папок через fallback-safe wrappers `fbShowModal/fbHideModal`
	- додано graceful fallback на ручний open/close модалки, якщо Bootstrap instance не ініціалізувався коректно
- `templates/server_audit.html` + `static/css/server_audit.css`:
	- додано окремий дизайн-шар для server audit (оформлення пошуку/фільтр-кнопки/dropdown, таблиці, hover-схем і mobile-адаптації)
	- для desktop server audit увімкнено власний внутрішній скрол таблиці (без прокрутки всієї сторінки), зі sticky table header
	- надалі механіку server audit вирівняно з admin audit: стандартна пагінація `prev/next + current/pages`, `limit=200`, без `per-page` selector та без локального persist-стану
	- прибрано ефект "переливання" у server audit: градієнти панелі/контролів/dropdown замінено на статичні фони
- `static/js/server_audit.js`:
	- логіку завантаження/фільтрації/пагінації переведено на той самий runtime-патерн, що використовується в `static/js/admin.js` для вкладки Audit
	- для сторінки server audit дефолтне завантаження змінено на останні `50` записів (`limit=50`)
	- зроблено SPA-safe DOM прив’язку до активної вкладки (`.yu-main`) і fallback-resolve `server_id` з URL, щоб Audit стабільно працював після переходів між sidebar tabs
	- виправлено резолв `server_id`: пріоритет `data-yu-spa-path`/`location.pathname` над глобальною змінною, щоб при переході через sidebar Audit не підхоплював застарілий id
- `templates/settings.html` + `templates/files.html`:
	- додано `window.YU_SERVER_ID = '{{id}}'` для узгодженого контексту server-id у всіх server вкладках під час SPA навігації
- `templates/_server_sidebar.html`:
	- додано новий пункт `Audit` у server sidebar
- `static/js/admin.js`, `static/js/dashboard.js`, `static/js/files.js`, `templates/admin_edit.html`:
	- ручні переходи (`history.pushState` / `location.href`) переведено на `yuRouteUrl`/`yuGo` з fallback, щоб уніфікувати hash-маркування шляху користувача

### Files page — directory-aware hash
- `static/js/files.js`:
	- для `/servers/{id}/files` hash тепер зберігає лише поточну директорію (без префікса `servers-{id}-files`), root-папка лишається без hash
	- при кожному переході між папками (HTMX swap) hash синхронізується з `data-path` поточного file browser
	- при відкритті сторінки шлях з hash відновлюється і браузер одразу завантажує саме цю директорію

### Files editor safety — non-editable file types
- `src/handlers/files.rs`:
	- додано backend-класифікацію `is_editable_file(...)` для non-editable типів (jar/war/ear, архіви, binary/media/office/font/database файли)
	- у file list HTML для non-editable файлів прибрано `href` на editor і додано `data-editable="false"`
	- у JSON listing додано поле `editable`, щоб frontend коректно рендерив нові рядки без editor-navigation
	- у `edit_file_page` та `save_file_content` додано guard: non-editable файли не відкриваються/не зберігаються через text editor
- `static/js/files.js`:
	- context menu `Edit` тепер недоступний для `data-editable="false"`
	- динамічні рядки (`fbBuildRow`) для non-editable файлів рендеряться без переходу в editor
- `static/css/files.css`:
	- додано візуальний read-only індикатор для non-editable рядків у file browser


# Інтеграція Alpine Linux
- Переробка `yunexal-setup` для Alpine/OpenRC потоку: видалено припущення systemd/get.docker.com/apt і перетворено на `apk`, `rc-service` і `rc-update` шляхи.
- Додано генерацію OpenRC-сервісу при налаштуванні (`/etc/init.d/yunexal-panel`) із підтримкою launcher-скрипту і конфіграційним шляхом nginx для Alpine (`/etc/nginx/http.d/`).
- Додано ISO scaffold для білдування: `scripts/iso/build-installer.sh`, `scripts/iso/mkimg.yunexal.sh` та `scripts/iso/genapkovl-yunexal.sh`.
- Додано musl-only helper релізу `scripts/release/build-musl.sh` для примусу non-glibc Linux артефактів.
- Оновлено документацію щодо напрямку Alpine інсталятора і додано документацію ISO-білду в `README.md` і `scripts/iso/README.md`.
- Оновлено `.gitignore` для ігнорування вихідних директорій ISO-білду (`/.build/`, `/out/`).
- Додано helper `yunexal-install` до ISO overlay з діями `prepare` та `finalize` для безпечного GPT розділення і монтування постійних даних.
- Додано перевірки безпечної політики диску інсталятора, що блокують поточний/кореневий диск за замовчуванням і вимагають явного `--mode force` для обходу.
- Додано необхідні пакети інсталятору (`gptfdisk`, `dosfstools`) у користувацький ISO профіль і список пакетів overlay world.
- Додано локальні скрипти workflow базового ISO: `scripts/iso/fetch-base-iso.sh`, `scripts/iso/prepare-custom-tree.sh` та `scripts/iso/repack-custom-iso.sh`.
- Завантажено і розпаковано офіційний Alpine latest-stable `x86_64` ISO в `.build/alpine-base/` для прямої в-workspace кастомізації дистрибутива.
- Додано пропатчування custom-tree для інжекції `apkovl=` параметра boot та вбудовування Yunexal overlay у перепакований гібридний BIOS/UEFI ISO.
- Додано модуль абстракції хост-команд `src/host.rs` із детекцією init-системи (`systemd`/`openrc`/`sysv`) і резолюцією шляхів команд.
- Переробка Docker потоку перезавантаження daemon у admin API для автоселекту service-команди за хост-ОС (`systemctl`, `rc-service` або `service`) замість тільки Ubuntu-орієнтованого `systemctl`.
- Переробка генерації sudoers fix-команди для Docker/UFW/storage операцій для використання дозволених бінарних шляхів замість hardcoded Ubuntu-шляхів.
- Оновлено відповіді UFW permission-fix у обох admin і networking handlers для використання хост-aware sudoers записів.
- Розширена абстракція привілеїв за допомогою `host::privileged_command` і root-детекції, тому custom ISO/root сесії виконують admin-команди прямо, а non-root сесії використовують `sudo -n` коли доступно.
- Мігровано admin/network/storage привілейовані операції від hardcoded `sudo` інвокацій до спільних хост-aware враперів і розв'язаних шляхів інструментів.
