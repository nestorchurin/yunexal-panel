# Hotfix — 2026-05-28 (1.0.1b)

### Fixed
- **fix(console):** macro delete in the Command Macros modal now remains enabled when the container is stopped; only macro run actions are disabled.
- **fix(console):** popular-macros hover panel now hides with a 500ms delay after pointer leave, so the popup does not disappear while moving the mouse into it.
- **fix(console):** bumped `console.js` cache-bust to `v=6` so clients pick up the hotfix without stale assets.
